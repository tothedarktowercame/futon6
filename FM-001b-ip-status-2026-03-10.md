# FM-001b IP encoding status — 2026-03-10

Status check requested on #math (tickle → codex). Encodings live in `data/frontiermath-pilot/FM-001b-ip-encoding.py`; logs below summarize the latest run artifacts for n ∈ {11, 23, 50}.

| n | q = 2n-1 | Log artifact | Solver outcome | Notes |
|---|-----------|--------------|----------------|-------|
| 11 | 21 | `FM-001b-ip-encoding-n11.log` | **Infeasible** (CBC proves no witness) | Run completed 74.9 s (CBC). Script prints `Status: Infeasible`, so 2-block-circulant ansatz fails for n=11. |
| 23 | 45 | `FM-001b-ip-encoding-n23.log` | **Incomplete** (logs stop mid-feasibility pump) | File contains only CBC feasibility-pump passes (≈8 s) with no `Status:` line or witness dump, so job was interrupted before branch-and-bound concluded. Need rerun. |
| 50 | 99 | `FM-001b-ip-encoding-n50.log` | **Not started** | Log only shows Python SyntaxWarning banner; solver never emitted build or status lines, so harness likely aborted immediately. |

## Follow-up actions
1. Rerun `python3 data/frontiermath-pilot/FM-001b-ip-encoding.py 23` with CBC (or another MILP solver) and commit the resulting log/witness decision.
2. For n=50, schedule a full solve (expect O(q^2) products ≈ 9801 big-M vars); plan to run on a longer-lived worker or split constraints before attempting again.
3. After reruns, capture the decision (`Status: Infeasible` / witness sets) inside this log directory so T2-single can pull without REPL access.
