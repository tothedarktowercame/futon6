;; GENERATED by scripts/bv_comb_typer.py — BV typing of the :composes
;; wiring skeleton + combs + gap-list (eustress). E-miner-v2-bv-combs.
{:bv/connectives
 {:seq "⟨ ; ⟩ non-commutative — models :composes (before/after)"
  :copar "( , ) commutative tensor — would model concurrent checkpoints"
  :par "[ , ] commutative — would model alternative branches"}
 :typed-missions
 [
  {:mission "M-first-flights"
   :shape :linear-chain          ;; no forks/joins -> seq only
   :bv-type {:bv/seq [:ckpt-00 :ckpt-01 :ckpt-02 :ckpt-03 :ckpt-04 :ckpt-05 :ckpt-06 :ckpt-07 :ckpt-08 :ckpt-09 :ckpt-10 :ckpt-11 :ckpt-12 :ckpt-13 :ckpt-14 :ckpt-15 :ckpt-16 :ckpt-17 :ckpt-18 :ckpt-19 :ckpt-20 :ckpt-21 :ckpt-22]}
   :composes-typing
   [[:ckpt-00 :ckpt-01 :gap-no-boundary-type] [:ckpt-01 :ckpt-02 :gap-no-boundary-type] [:ckpt-02 :ckpt-03 :gap-no-boundary-type] [:ckpt-03 :ckpt-04 :gap-no-boundary-type] [:ckpt-04 :ckpt-05 :gap-no-boundary-type] [:ckpt-05 :ckpt-06 :gap-no-boundary-type] [:ckpt-06 :ckpt-07 :gap-no-boundary-type] [:ckpt-07 :ckpt-08 :gap-no-boundary-type] [:ckpt-08 :ckpt-09 :gap-no-boundary-type] [:ckpt-09 :ckpt-10 :gap-no-boundary-type] [:ckpt-10 :ckpt-11 :gap-no-boundary-type] [:ckpt-11 :ckpt-12 :gap-no-boundary-type] [:ckpt-12 :ckpt-13 :gap-no-boundary-type] [:ckpt-13 :ckpt-14 :gap-no-boundary-type] [:ckpt-14 :ckpt-15 :gap-no-boundary-type] [:ckpt-15 :ckpt-16 :gap-no-boundary-type] [:ckpt-16 :ckpt-17 :gap-no-boundary-type] [:ckpt-17 :ckpt-18 :gap-no-boundary-type] [:ckpt-18 :ckpt-19 :gap-no-boundary-type] [:ckpt-19 :ckpt-20 :gap-no-boundary-type] [:ckpt-20 :ckpt-21 :gap-no-boundary-type] [:ckpt-21 :ckpt-22 :gap-no-boundary-type]]
   :endpoints-comb
   {:form "⟨:ckpt-00 ; - ; :ckpt-22⟩" :hole-dom nil :hole-cod 3 :status :skeleton-only}}
  {:mission "M-futon1a-rebuild"
   :shape :linear-chain          ;; no forks/joins -> seq only
   :bv-type {:bv/seq [:ckpt-2026 :ckpt-01 :ckpt-02 :ckpt-03 :ckpt-04]}
   :composes-typing
   [[:ckpt-2026 :ckpt-01 :gap-no-boundary-type] [:ckpt-01 :ckpt-02 :gap-no-boundary-type] [:ckpt-02 :ckpt-03 :gap-no-boundary-type] [:ckpt-03 :ckpt-04 :typed-monotone]]
   :endpoints-comb
   {:form "⟨:ckpt-2026 ; - ; :ckpt-04⟩" :hole-dom nil :hole-cod 82 :status :skeleton-only}}
  {:mission "M-operational-readiness"
   :shape :linear-chain          ;; no forks/joins -> seq only
   :bv-type {:bv/seq [:ckpt-00 :ckpt-01 :ckpt-02 :ckpt-03 :ckpt-04]}
   :composes-typing
   [[:ckpt-00 :ckpt-01 :typed-monotone] [:ckpt-01 :ckpt-02 :typed-monotone] [:ckpt-02 :ckpt-03 :typed-monotone] [:ckpt-03 :ckpt-04 :gap-no-boundary-type]]
   :endpoints-comb
   {:form "⟨:ckpt-00 ; - ; :ckpt-04⟩" :hole-dom 686 :hole-cod nil :status :skeleton-only}}
  {:mission "M-pilot-appearance"
   :shape :linear-chain          ;; no forks/joins -> seq only
   :bv-type {:bv/seq [:ckpt-00 :ckpt-01 :ckpt-02 :ckpt-03]}
   :composes-typing
   [[:ckpt-00 :ckpt-01 :gap-no-boundary-type] [:ckpt-01 :ckpt-02 :gap-no-boundary-type] [:ckpt-02 :ckpt-03 :gap-no-boundary-type]]
   :endpoints-comb
   {:form "⟨:ckpt-00 ; - ; :ckpt-03⟩" :hole-dom nil :hole-cod nil :status :skeleton-only}}
 ]
 :gap-list   ;; the load-bearing eustress deliverable
 [
  {:gap :gap-no-typed-interface
   :finding "29/33 :composes edges cannot be interface-typed: :application nodes carry prose :witness but NO dom/cod object type. A verified BV comb needs each checkpoint's interface."
   :mining-feedback "miner: emit :interface {:in <type> :out <type>} per :application node (or a typed boundary end on :composes), not just :from/:to order."}
  {:gap :gap-boundary-type-prose-buried
   :finding "only 8/37 checkpoints expose ANY boundary proxy (the 'Test state: N tests' line); the rest are untyped. Even the proxy is partial and inconsistent across missions."
   :mining-feedback "miner: require a structured state field per checkpoint (:test-count :gates-met :capability-set) so the wire type is TOTAL."}
  {:gap :gap-no-par-copar
   :finding "all 4 skeletons are LINEAR chains (0 forks/joins); BV collapses entirely to seq. The deep-inference richness (seq interleaved with par/copar via the medial rule) is unexercisable. :jointly-with (n-ary parallel) is spec-only, never emitted."
   :mining-feedback "miner: detect dataflow-INDEPENDENT checkpoints and emit them as a par/copar bundle (or :jointly-with), not a forced linear chain."}
  {:gap :gap-authored-order-not-dataflow
   :finding "every :composes is :via 'in authored checkpoint order' — a temporal/narrative wire, NOT a typed morphism boundary. Sequential authoring does not imply a dataflow dependency."
   :mining-feedback "miner: distinguish temporal-order from dataflow-dependency edges; only the latter is a true :composes (the former may be par)."}
  {:gap :gap-no-unit
   :finding "no identity/unit checkpoint exists, so BV combs cannot be normalised (seq/par/copar need the unit ∘ for the laws)."
   :mining-feedback "miner: emit an explicit unit/no-op application where a phase begins, so combs have an identity to compose against."}
 ]
 :summary
  {:missions 4 :checkpoints 37 :composes-edges 33
   :typed-monotone 4 :gap-no-boundary-type 29 :type-mismatch 0
   :interface-verified-combs 0 :skeleton-only-combs 4}}
