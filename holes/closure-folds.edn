;; closure-folds.edn — structured fold records (input to cascade_learn.py / Build 2).
;; Each record: which LIBRARY patterns actually folded the hole (the graph-rewrite rules
;; applied), extracted from futon6/holes/early-closures.md. `used` = the subset of the
;; cascade whose THEN-clauses did the fold; `success` = did it fold. This is the dense,
;; real, peradam-free learning signal (pattern-utility + co-usage for edge-seeding).
;;
;; RECORDING DISCIPLINE (claude-1, 2026-06-10 — the β / discrimination unlock): record FAILED folds too.
;; The signal is only dense if BOTH outcomes are written. Today this file is survivorship-biased (5
;; success, 0 failure) → every posterior can only go UP (β can't grow), so the grounded A/B is stuck at
;; the uniform-0.667 ceiling. When a fold-attempt's `:used` set does NOT close its hole, write a record
;; with `:success false` + that `:used` (+ evidence of the attempt). That recorded NEGATIVE is the β
;; term — the amortization principle ("if no, we still learned something") made data. cascade_learn
;; already consumes `:success false` (used-and-didn't-close → β+=1; failed pairs never seed an edge).
[{:scope "recommendation-bindings/q5" :used [] :success true :note "investigation closure, no fold"}
 {:scope "kit-outbox" :success true
  :used ["scan-coherence/mission-anchored-scan" "correspondence-coherence/mission-unlocks-eoi"]}
 {:scope "aif2/inv-tripwire-mapping" :success true
  :used ["sidecar/typed-kolmogorov-arrows"]}
 {:scope "kit-intake" :success true
  :used ["dsc/evidence-situated-log"]
  :note "+ registry §8.1 two-part guard (apparatus, not a library pattern)"}
 {:scope "kit-cadence" :success true
  :used ["ukrns/model-recompute-schedule" "devmap-coherence/prototype-maturity-lifecycle"]}
 {:scope "E-mission-head/head-sigil" :success false :used []
  :note "v0 FAILED fold (the file's first recorded negative, per claude-1's β discipline): default whole-text representation, no pattern guidance — xenotype uncomputable, bit-confidence 0.29 (4/8 bits coin-flips). Evidence: futon6/holes/missions/E-mission-head.md §3.1; futon5/scripts/head_exotype_probe.py"}
 {:scope "E-mission-head/head-sigil" :success true
  :used ["futon-theory/mission-interface-signature"]
  :note "v0.1 fold: HEAD recast as IF/HOWEVER/THEN/BECAUSE (Golemization) → xenotype-32 computable, conf 0.33, THEN/BECAUSE anchor cos≈0.46. + 2 candidate patterns did real work but are NOT in the library (apparatus-grade, kit-intake precedent): candidate-pattern/two-projections-of-one-quantity, candidate-pattern/stable-theme-enables-free-improvisation — minted :open in meme.db cascade/E-mission-head-argue"}

 ;; ----- FIRST PATTERN-β (claude-3, 2026-06-10) — complements claude-4's E-mission-head v0 negative
 ;; above (which is :used [] → moves no PATTERN posterior). These two attach β to a REAL pattern, so a
 ;; posterior moves off the uniform 0.667 — the actual discrimination demo. Method (claude-4's gate
 ;; honoured): the phylogeny-greedy cascade ALREADY overlays the full 2,538-edge phylogeny; on both
 ;; holes it saturated at size 1, edge_count=0, coverage_gap=true — the phylogeny was searched for a
 ;; combiner and found none, so this is 'the patterns didn't fold it', NOT 'I missed the structure'
 ;; (Closure-01's escape hatch checked + empty). The sole match is a cosine artifact on 'operator':
 ;; math-formalization/continuous-linear-map-composition is a Lean bounded-operator proof (audience:
 ;; functional-analysis-students); the hole needs an AIF-architecture ARGUE/DERIVE for the Click/Tick
 ;; capability machine. β attaches to the mis-selected pattern (a cosine attractor); selector quality
 ;; is a separate surface.
 {:scope "hypergraph-operator/argue" :success false
  :used ["math-formalization/continuous-linear-map-composition"]
  :missing "aif-architecture/capability-operator-argue"
  :note "cosine artifact on operator; Lean functional-analysis pattern vs AIF-architecture hole; phylogeny-greedy saturated size-1 edge_count=0 so no pattern-language to compose"}
 {:scope "hypergraph-operator/derive" :success false
  :used ["math-formalization/continuous-linear-map-composition"]
  :missing "aif-architecture/capability-operator-derive"
  :note "second independent instance of the same cosine artifact; same size-1 edge_count=0 saturation"}
 {:scope "E-mission-head/verify-v0" :success true
  :used ["mission-coherence/logic-model-before-code"]
  :note "VERIFY V0: 5-invariant core.logic+pldb model (mission_head_invariants.clj), witness clean + 5/5 adversarials caught; the pattern was prescribed by the mission's own ARGUE cascade — cascade walking its talk"}
 {:scope "E-mission-head/health-readout" :success true :used []
  :note "H1 fold (codex-1, futon5@17e9b53): --emit-health producer; apparatus-grade (the bridge), no library-pattern THEN-rewrites; fable-1 review PASS"}
 {:scope "E-mission-head/seeded-beliefs" :success true
  :used ["peripherals/read-existing-seam-before-implementing"]
  :note "H2 fold (codex-2, futon3c@9c4033b): the M-aif-head REVIVE — seam read + wired, not rebuilt; A1 as runtime check-law; review fix: degraded?-checks-xenotype"}
 {:scope "E-mission-head/lifeform-lane" :success true :used []
  :note "H3 fold (codex-3, futon3c@5b87192): vitals block; rendered live from health.json; fable-1 review PASS"}]
