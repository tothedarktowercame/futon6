;; CLean produced from an IATC argument-graph by scripts/iatc_to_clean.py.
;; Mechanical except :method / :clean/shape :macro, which LLaMA types
;; (see the .prompt.txt). typing-source recorded below.
{:clean/proof "0708.3398__p22"
 :clean/source {:iatc "0708.3398__p22"}
 :clean/typing-source :llama
 :clean/seq [:reduce-to-known-result :reduce-to-known-result]
 :clean/boxes
 [{:id :e-chain-construction :method :reduce-to-known-result
   :text "there is a chain of three morphisms bgK^u -> R3(bgK^u) <- R3L2(bgK^u) -> R3L2R1(bgK^u) with C-contraacyclic cones"
   :consumes [:bgK-def]
   :produces :chain-of-morphisms
   :hole {:kind :sorry :discharge :sorryProof :satiety :payoff :wanted "functorial-construction-of-acyclic-cones"}}
  {:id :e-preservation :method :reduce-to-known-result
   :text "when bgK^u is semiinjective, all complexes in the chain are also semiinjective complexes of (A-injective, C-coinjective, or semiinjective) S-semimodule s"
   :consumes [:chain-of-morphisms :bgK-def]
   :produces :semiinjective-preservation
   :hole {:kind :sorry :discharge :sorryProof :satiety :payoff :wanted "semiinjective-complexes-preserved-under-acyclic-cone-construction"}}]
 :clean/wires
 [{:from :e-chain-construction :to :e-preservation :carries :chain-of-morphisms}]
 :clean/copar [{:reading :informal :is :clean/seq}
               {:reading :formal   :is [:clean/boxes :clean/wires]}]
 :clean/shape {:macro :reduce-to-known
               :holes-at [:e-chain-construction :e-preservation]
               :discharges-at []}}
