;; CLean — R12 (dual-loop / hyperparameter inference) decomposed into its sub-boxes. REFRAMED
;; after claude-5's pattern-grain result (zai-13 + independent verification): the accumulation
;; corpus EXISTS at pattern grain (co-application), so R12 FILLS — but honestly, via EMBEDDING-
;; CLUSTERING + BMR-EIG, NOT naive count-BMR merges (which over-merge at 94-dim). This is the
;; load-bearing negative result of the "AIF and Embeddings" thesis. clean_argcheck-valid.

{:clean/proof "R12-decomposition"
 :clean/title "R12: dual-loop apparatus + accumulation — FILLED via embedding + BMR-EIG"
 :clean/source {:requirement "R12"
                :status "FILLED (honest): concepts = embedding constellations; epistemic value = BMR-EIG; count-BMR corroborates weakly, insufficient alone"
                :commit "4064533+8200f08+81e231c(structure-learning-explainer,claude-5)"}

 :clean/seq [:dual-loop-apparatus :accumulate :concept-formation]

 :clean/boxes
 [{:id :b1 :method :dual-loop-apparatus
   :text "The dual-loop (two-layer calibration) apparatus: fast loop (per-tick belief update + EFE scoring) + slow loop (hyperparameter/precision calibration over a window), wired structurally. two-layer-calibration, evidence-precision-registry. Built, apparatus-only."
   :produces :dual-loop-structure}
  {:id :b2 :method :accumulate
   :text "The accumulation the slow loop learns from. RESOLVED at pattern grain: the pattern co-application corpus EXISTS (6903 pairs / 2721 weighted edges) — dense real co-occurrence, unlike the bijective capability star-map (0 co-occurrence, degenerate). The data flows; the open question moves to the mechanism (b3)."
   :produces :accumulated-co-occurrence}
  {:id :b3 :method :concept-formation
   :text "Slow-loop generative-model revision. HONEST RESULT (claude-5, verified): naive count-BMR over the raw corpus is MISCALIBRATED in high-dim — at 94-dim all 6903 pairs accept (ΔF -6.5..-33.7) and collapse to a single concept even for DISJOINT patterns (the complexity term inflates with outcome-dimension). Structure is instead recovered by EMBEDDING-CLUSTERING: the concepts are Joe's semilattice CONSTELLATIONS (P0-P17, embedding + citation-overlap), and embedding-gated BMR (cos >= 0.7, cutoff -20) recovers them (AIF, math-proof, engineering). BMR's genuine contribution is the EPISTEMIC layer: EIG per constellation {P -> posterior stddev} (proportional to 1/sqrt(alpha0); singletons 0.029 most-uncertain -> P2/AIF 0.009 most-settled) -- the layer the constellations lacked -- plus WEAK corroboration (within-constellation pairs merge 1.55 delta-F stronger than across; hub patterns like stop-the-line bridge constellations and weaken it). Structure learning is real via embedding + BMR-EIG; count-BMR alone is insufficient. Discharges the R12 want-signature."
   :consumes [:dual-loop-structure :accumulated-co-occurrence]
   :produces :revised-generative-model-with-epistemic-value
   :discharges {:to :hyperparameter-inference-from-accumulated-evidence}}]

 :clean/wires
 [{:from :b1 :to :b3 :carries :dual-loop-structure}
  {:from :b2 :to :b3 :carries :accumulated-co-occurrence}]

 :clean/copar [{:reading :informal :is :clean/seq}
               {:reading :formal   :is [:clean/boxes :clean/wires]}]

 :clean/shape {:macro :dual-loop-with-accumulation
               :holes-at []
               :discharges-at [:b3]
               :note "FILLED -- all three boxes built. b1 apparatus checkmark; b2 accumulate checkmark (pattern co-application corpus exists, dense-real); b3 concept-formation checkmark via embedding-clustering + BMR-EIG. The honest reframe: NOT 'BMR merges fired' (naive count-BMR over-merges at scale -- a load-bearing NEGATIVE result for the 'AIF and Embeddings' thesis: the metric space makes structure learning tractable where pure BMR-over-counts collapses), but 'concepts = embedding constellations; epistemic value = BMR-EIG; count-BMR corroborates weakly.' The honest verb is structure-RECOVERY + epistemic-quantification, not structure-learned-from-scratch (constellations are embedding-derived, semi-authored by Joe's semilattice). RECOVERY CEILING now EVIDENCED (claude-5/zai-13): the one candidate coarser-merge (AIF-12, C2+C8+C1, cosine-gated) was falsification-tested against two embedding-independent signals and REJECTED -- raw mission-Jaccard co-occurrence (0.400 within vs 0.163 cross, 2.5x weaker) + live cascade co-application (57.1% vs 8.6%; added members 0/10); cosine adjacency != co-occurrence, constellation granularity VINDICATED; the right method is embedding + co-occurrence JOINTLY (= Joe's constellations, embedding + citation-overlap). DEFERRED and OUT OF R12's scope: wiring {constellation -> EIG} into the ranker at pattern grain (claude-4's grain-agnostic :eig-fn + claude-5's eig-for-produces re-keyed to patterns) -- that inhabits the R5-EIG / R13-signal boxes, not this one. Zero unbuilt boxes."}}
