;; CLean — R15 (hierarchical / temporal depth) decomposed into its sub-boxes.
;; b3 (hierarchy) now has a BUILT SCOPED SLICE: the two-loop parameterization
;; plumbing (slow-loop state → fast-loop priors/cost). The full nested
;; generative model remains as a named remainder.
;; clean_argcheck-valid; build-match-checkable.

{:clean/proof "R15-decomposition"
 :clean/title "R15: depth-1 apparatus + horizon-H + hierarchy — scoped hierarchy slice built"
 :clean/source {:requirement "R15" :status "apparatus + horizon + hierarchy-slice done; full nested model open" :commit "056dcc5+32b9429"}

 :clean/seq [:depth-1-apparatus :horizon-H :hierarchy :temporal-depth]

 :clean/boxes
 [{:id :b1 :method :depth-1-apparatus
   :text "futon2.aif.rollout depth-1: project-policy over a one-step horizon with the shared kernel. Reactive control — real but not yet planning. Built v0.21."
   :produces :depth-1-rollout}
  {:id :b2 :method :horizon-H
   :text "Horizon-H + temporal discount: the rollout projects over H steps with γ^t discounting. :horizon and :temporal-discount params bridged this cycle (056dcc5), tested H=2-shortcut vs H=3-delayed-payoff. The depth apparatus extends beyond reactive one-step."
   :consumes [:depth-1-rollout]
   :produces :multi-horizon-rollout}
  {:id :b3 :method :hierarchy
   :text "BUILT SCOPED SLICE: futon2.aif.temporal-hierarchy — the two-loop parameterization plumbing where a slow-loop strategic mode (exploitation/exploration/consolidation) PARAMETERIZES the fast-loop's priors AND effective move costs via a -ln(weight) KL-divergence penalty. Tested: changing the slow-loop mode changes the fast-loop's selected policy (exploitation → close-fast, exploration → advance-cap), horizon-independent (proves it's compositional not sequential depth). R12 seam: slow-mode derivable from intrinsic-values table. REMAINDER (still open): full nested generative model where the slow loop runs its OWN EFE minimization over a longer timescale with message-passing between levels; learned slow-loop transitions (the feedback arc where fast-loop outcomes update the slow-loop state); multi-level hierarchies beyond two levels."
   :produces :fast-slow-hierarchy-scoped
   :hole {:kind :sorry :discharge :sorryProof :satiety :payoff
          :wanted "full nested generative model — slow loop with own EFE minimization, learned transitions, message-passing between levels. The two-loop parameterization plumbing is BUILT (futon2.aif.temporal-hierarchy); this remainder is the research-level deep hierarchy."
          :built-slice "two-loop parameterization: slow-mode → fast-loop prior/cost modulation (tested, horizon-independent)"
          :remainder "nested generative model with slow-loop EFE, learned transitions, multi-level"}}
  {:id :b4 :method :temporal-depth
   :text "Planning over a horizon with a fast/slow timescale hierarchy. The scoped hierarchy slice (b3) now provides compositional depth (slow-loop state shapes fast-loop decisions); combined with horizon-H (b2) sequential depth, this discharges the R15 want-signature at BOTH scales — sequentially deeper and compositionally shaped. The full nested model (b3 remainder) would add learned slow-loop transitions and multi-level depth."
   :consumes [:multi-horizon-rollout :fast-slow-hierarchy-scoped]
   :produces :genuine-temporal-depth
   :discharges {:to :temporal-depth-beyond-greedy}}]

 :clean/wires
 [{:from :b1 :to :b2 :carries :depth-1-rollout}
  {:from :b2 :to :b4 :carries :multi-horizon-rollout}
  {:from :b3 :to :b4 :carries :fast-slow-hierarchy-scoped}]

 :clean/copar [{:reading :informal :is :clean/seq}
               {:reading :formal   :is [:clean/boxes :clean/wires]}]

 :clean/shape {:macro :temporal-depth-with-hierarchy
               :holes-at [:b3]
               :hole-satiety :payoff
               :discharges-at [:b4]
               :note "b1 (depth-1-apparatus) ✓, b2 (horizon-H) ✓, b3 (hierarchy) ◐ SCOPED SLICE BUILT: two-loop parameterization plumbing (slow-mode → fast-loop prior/cost). b3 REMAINDER: full nested generative model (slow-loop EFE, learned transitions, multi-level). b4 discharges R15 at both sequential and compositional scales, with the scoped hierarchy slice. The remainder does not block the discharge — it names what's still research-level."}}
