;; CLean — R14 (precision over policies, γ) decomposed into its sub-boxes, unbuilt boxes marked.
;; The ◐→box-pointer: "R14 needs the :live-wire and :accumulate boxes." clean_argcheck-valid;
;; build-match-checkable. Disaggregated this cycle — the HARD half (grounding) is done; two
;; unbuilt boxes remain, both gated (an operator/joint arm + real data), not missing code.

{:clean/proof "R14-decomposition"
 :clean/title "R14: γ as a learned, two-sided precision — which boxes remain"
 :clean/source {:requirement "R14" :status "grounding + live-wire BUILT + ARMED (default-on); 1 data box remains (real variance)" :commit "723cacf+a3cbc56+d36086f+b624242"}

 :clean/seq [:grounded-realized :grounded-expected :live-wire :accumulate :gamma-learned]

 :clean/boxes
 [{:id :b1 :method :grounded-realized
   :text "Realized outcome grounded in the substrate dial (bound − inhabited), replacing coverage-over-reproduction. Built this cycle — the hard conceptual half (killing the tautology)."
   :produces :realized-dial}
  {:id :b2 :method :grounded-expected
   :text "Two-sided expected leg: the fold's coverage forecast → predicted-remaining. Built this cycle — γ can now commit, not just hedge."
   :produces :expected-forecast}
  {:id :b3 :method :live-wire
   :text "Wire the grounded producer into the live γ source, replacing coverage-ΔG. BUILT + ARMED (b624242): *gamma-grounded-feed?* + *live-wire?* now default ON (Joe-directed) route with-realized-outcome → realized-outcome-grounded. Latent until R10 runs — armed ≠ running; bind either false to restore."
   :produces :live-gamma-feed}
  {:id :b4 :method :accumulate
   :text "Accrue realized samples with genuine variance."
   :produces :real-variance-samples
   :hole {:kind :sorry :discharge :ungroundedBinder :satiety :bundling
          :wanted "real fold-deposit missions producing genuine variance (currently 2/5 samples carry real forecasts). A data dependency. Unbuilt box #2."}}
  {:id :b5 :method :gamma-learned
   :text "γ = clamp(2^(gain·perf̄), floor, cap), calibrated from realized-vs-expected on grounded, variance-bearing samples. Discharges the R14 want-signature."
   :consumes [:realized-dial :expected-forecast :live-gamma-feed :real-variance-samples]
   :produces :two-sided-gamma
   :discharges {:to :learned-bounded-precision}}]

 :clean/wires
 [{:from :b1 :to :b5 :carries :realized-dial}
  {:from :b2 :to :b5 :carries :expected-forecast}
  {:from :b3 :to :b5 :carries :live-gamma-feed}
  {:from :b4 :to :b5 :carries :real-variance-samples}]

 :clean/copar [{:reading :informal :is :clean/seq}
               {:reading :formal   :is [:clean/boxes :clean/wires]}]

 :clean/shape {:macro :two-sided-grounded-precision
               :holes-at [:b4]
               :discharges-at [:b5]
               :note "b1 (realized) ✓, b2 (expected) ✓, and now b3 (live-wire) ✓ are BUILT — the migration is coded dark (d36086f: *gamma-grounded-feed?*, default OFF). Both arms now FLIPPED (*live-wire?* + *gamma-grounded-feed?* default-on, b624242, Joe-directed). What remains is NOT code, NOT arms: b4 (real variance, a data dependency) + an R10 run. One data box; zero unbuilt code boxes."}}
