# Peradam Certificates

This directory is the file-first certificate store for
`M-peradam-mechanization`.

No real certificate is issued in this parcel.  The loader in
`src/futon6/peradam_cert.py` accepts only structured witness records and
refuses prose-era evidence with a named cause.
