# FM-XXX Problem State

## Metadata

- `problem_id`:
- `title`:
- `source_url`:
- `owner`:
- `started_utc`:

## Spec-Lock

- `formal_statement`:
- `quantifiers`:
- `parameter_regime`:
- `output_format`:
- `forbidden_substitutions`:
- `spec_lock_status`: `pass` / `fail`

## Current Hypothesis

- `answer_hypothesis`: `YES` / `NO` / `unknown`
- `confidence`: `low` / `medium` / `high`

## Risk Flags

- `spec_risk`:
- `no_bias_risk`:
- `domain_depth_risk`:
- `verification_risk`:

## Mode

- `current_mode`: `SPEC` / `FALSIFY` / `CONSTRUCT` / `VERIFY` / `MAP`
- `mode_transition_reason`:

## Opposite-Answer Cycle

- `opposite_answer_attempted`: `yes` / `no`
- `falsification_artifact`:
- `result`:

## Open Lemmas

1.
2.
3.

## Verification

- `symbolic_checks`:
- `computational_checks`:
- `adversarial_checks`:
- `dependency_audit`:

## TryHarder Log

1. License ID:
   - target_claim:
   - new_lever:
   - witness:
   - kill_condition:
   - outcome:

## Final Status

- `status`: `CLOSES` / `NARROWS` / `FAILS` / `OPEN-LEMMA-LOCALIZED`
- `final_note`:
- `next_action`:

