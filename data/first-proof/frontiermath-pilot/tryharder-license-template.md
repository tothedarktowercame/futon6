# TryHarder License

- `license_id`:
- `problem_id`:
- `issued_by`:
- `issued_utc`:
- `timebox_minutes`:

## Required Justification

1. `target_claim`:
   - One specific claim ID only.

2. `bottleneck_type`:
   - `missing-lemma` / `obstruction-search` / `spec-ambiguity` / `dependency-gap` / `computation-gap`

3. `new_lever`:
   - What is materially different from previous failed cycle?

4. `witness`:
   - What concrete output must exist by end of timebox?

5. `kill_condition`:
   - What exact condition triggers forced mode switch?

## Approval

- `approved`: `yes` / `no`
- `approval_reason`:

## Outcome

- `witness_met`: `yes` / `no`
- `mode_after`: `CONSTRUCT` / `VERIFY` / `MAP` / `FALSIFY` / `SPEC`
- `artifact_links`:
- `notes`:

